import classes from "./form.module.css";

const Form = () => {
  return (
    <section className={classes.formSection}>
      <div className="container"></div>
    </section>
  );
};

export default Form;
